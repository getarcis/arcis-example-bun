{
  "name": "arcis-example-bun",
  "version": "1.0.0",
  "description": "Minimal Bun + Hono + Arcis app. Demonstrates the @arcis/node/bun adapter (Hono middleware) blocking 20+ attack vectors with one app.use() line.",
  "private": true,
  "type": "module",
  "main": "server.ts",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --watch run server.ts",
    "attack": "bun run attack.ts"
  },
  "license": "MIT",
  "author": "Gagan CM",
  "dependencies": {
    "@arcis/node": "^1.5.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^6.0.3"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
